<div class="box">
  <h2>Create User</h2>
  <p class="lead">Create a new user with assigning a new role</p>
  <p class="text-muted">Pellentesque habitant morbi tristique senectus et netus et malesuada fames ac turpis egestas.</p>

  <%= form_for @user do |f| %>
    <%= render 'shared/errors', obj: @user %>

    <div class="row">
      <div class="col-md-6">
        <div class="form-group">
          <%= f.label :email %>
          <%= f.email_field :email, autocomplete: 'off', class: 'form-control' %>
        </div>
      </div>
    </div>

    <div class="row">
      <div class="col-md-6">
        <div class="form-group">
          <%= f.label :password %>
          <%= f.password_field :password, class: 'form-control' %>
        </div>
      </div>
      <div class="col-md-6">
        <div class="form-group">
          <%= f.label :password_confirmation %>
          <%= f.password_field :password_confirmation, class: 'form-control' %>
        </div>
      </div>
    </div>

    <div class="row">
      <div class="col-12">
        <div class="form-group">
          <%= f.label :role %>
          <%= f.select :role, options_for_select(Role.all.pluck(:name), @user.role),
                       {}, { class: 'form-control' } %>
        </div>
      </div>
    </div>

    <div class="col-md-12 text-center">
      <%= link_to dashboard_index_path, class: 'btn btn-outline-primary' do %>
        <i class="fa fa-close"></i> Cancel
      <% end %>
      <button type="submit" class="btn btn-primary">
        <i class="fa fa-save"></i> Create
      </button>
    </div>
  <% end %>
</div>
