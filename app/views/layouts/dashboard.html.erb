<!DOCTYPE html>
<html>
  <head>
    <title>Bigshoutz</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="description" content="">
    <%= favicon_link_tag 'favicon.ico' %>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="robots" content="all,follow">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto:100,300,400,700">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= render 'shared/header' %>

    <div class="notice-section">
      <div id="info-notice">
        <%= render 'shared/notices', type: 'info', notice: notice %>
      </div>

      <div id="warning-notice">
        <%= render 'shared/notices', type: 'warning', notice: alert %>
      </div>
    </div>

    <div id="all">
      <div id="content">
        <div class="container">
          <div class="row">
            <div class="col-3">
              <%= render 'shared/sidebar' %>
            </div>
            <div class="col-9">
              <%= yield %>
            </div>
          </div>

          <% if params[:controller] == 'listings' && params[:action] == 'show' %>
            <div class="row" id="listing-comments">
              <%= render 'comments/all', comments: @listing.comments %>
            </div>
          <% end %>
        </div>
      </div>
    </div>

    <%= render 'shared/footer' %>
    <%= render 'shared/copyright' %>
  </body>
</html>
